# NOTE: _redirects is NOT processed when _worker.js is present (Advanced Mode).
# Routing is handled by the worker. This file is kept for reference.
# When not using Advanced Mode, uncomment below:
# /assets/*  /assets/:splat  200
# /*  /index.html  200
